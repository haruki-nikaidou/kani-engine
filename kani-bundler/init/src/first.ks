; Entry point — first file executed when the game starts.

*start

; Load shared macro definitions (endreplay, etc.)
@call storage="tyrano.ks"

; Jump to the title screen
@jump storage="title.ks"

[s]
